{
    // Visit https://aka.ms/tsconfig to read more about this file
    "compilerOptions": {
        "target": "es2022", // Specifies the ECMAScript target version for the output JavaScript.
        "module": "NodeNext", // Specifies the module code generation method, aligning with Node.js's module resolution.
        "moduleResolution": "NodeNext", // Specifies how modules are resolved, again aligning with Node.js.
        "outDir": "./dist", // Specifies the output directory for compiled JavaScript files.
        "esModuleInterop": true, // Enables interoperability between CommonJS and ES modules.
        "forceConsistentCasingInFileNames": true, // Ensures consistent casing in file names for better cross-platform compatibility.
        "strict": true, // Enables a wideß range of type-checking validation rules.
        "skipLibCheck": true, // Skips type checking of declaration files, improving compilation speed.
        "sourceMap": true, // Generates source map files for easier debugging.
        "typeRoots": ["./node_modules/@types"]
    },
    "include": ["server.ts"], // Specifies which files or directories should be included in the compilation.
    "exclude": ["node_modules", "dist"] // Specifies which files or directories should be excluded from the compilation.
}